import { sinRespuesta } from 'questions/helpers/sin-respuesta';
import { module, test } from 'qunit';

module('Unit | Helper | sin respuesta');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sinRespuesta([42]);
  assert.ok(result);
});
